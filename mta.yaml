_schema-version: 3.3.0
ID: SAP_PACE
description: A simple CAP project.
version: 1.0.0
modules:
- name: SAP_PACE-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: SAP_PACE-auth
  - name: SAP_PACE-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: SAP_PACE-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: SAP_PACE-db
  parameters:
    buildpack: nodejs_buildpack
- name: SAP_PACE-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: SAP_PACE-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sappaceuiservicecatalogservicecatalog.zip
      name: sappaceuiservicecatalogservicecatalog
      target-path: resources/
- name: sappaceuiservicecatalogservicecatalog
  type: html5
  path: app/service-catalog
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: SAP_PACE-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: SAP_PACE-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: SAP_PACE-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: SAP_PACE-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: SAP_PACE-html5-service
    service-plan: app-host
- name: SAP_PACE-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: SAP_PACE-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
